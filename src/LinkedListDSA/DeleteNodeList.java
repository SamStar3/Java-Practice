package LinkedListDSA;

public class DeleteNodeList {
//    public Node deleteNode(Node head, int x) {
//    if (head == null || x <= 0) return head;
//
//    Node current = head;
//
//    // Traverse to the node at position x
//    for (int i = 1; current != null && i < x; i++) {
//        current = current.next;
//    }
//
//    // If the node to be deleted is not found, return the head
//    if (current == null) return head;

    // If the node to be deleted is the head node
//    if (current == head) {
//        head = current.next;
//        if (head != null) {
//            head.prev = null;
//        }
//    } else {
//        // Change the next of the previous node
//        if (current.prev != null) {
//            current.prev.next = current.next;
//        }
//
//        // Change the prev of the next node
//        if (current.next != null) {
//            current.next.prev = current.prev;
//        }
//    }

//    return head;
//}
}